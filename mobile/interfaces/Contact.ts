//create an interface of contact with id, firstName, lastName, email, phoneNumber, and address
export interface Contact {
  id: string;
  firstName: string;
  lastName: string;
  email: string;
  phoneNumber: string;
  address: string;
}
